<div class = "row valign-wrapper">
  <h4 class="col s8 valign">Bins Listing</h4>
  <%= link_to "Add New Bin", new_bin_path, class: "waves-effect waves-light btn col s4 valign green white-text" %>
</div>

<% if @bins.empty? %>
    No bins created yet.
<% else %>
  <table class = "hoverable">
    <thead>
      <tr>
        <th>Date</th>
        <th>Agent</th>
        <th>Number of Bins</th>
<!--         <th>Item</th>
        <th>Quantity</th> -->
      </tr>
    </thead>
    <tbody>
      <% @bins.each do |bin| %>
        <tr>
          <td><%= link_to bin.checkout_date.strftime('%m/%d/%y'), bin_path(bin) %></td>
          <td><%= link_to bin.agency.name, agency_path(bin.agency) %></td>
          <td><%= bin.num_of_bins %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<!-- Add the pagination using will_paginate gem -->
<span class="paginate">
  <%= will_paginate @all_orders, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>


<!-- <table>
  <thead>
    <tr>
      <th>Checkout date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @bins.each do |bin| %>
      <tr>
        <td><%# bin.checkout_date %></td>
        <td><%# link_to 'Show', bin %></td>
        <td><%# link_to 'Edit', edit_bin_path(bin) %></td>
        <td><%# link_to 'Destroy', bin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table> 

<br> -->

<%# link_to 'New Bin', new_bin_path %>
