<div id='bin_results'>

  <% if @bins.empty? %>
      No bins created yet.
  <% else %>
    <table class = "hoverable">
      <thead>
        <tr>
          <th>Date</th>
          <th>Agency</th>
          <th>Program</th>
          <th>Number of Bins</th>
          <th>Approx Value of Bin</th>
        </tr>
      </thead>
      <tbody>
        <% @bins.each do |bin| %>
          <tr>
            <td><%= link_to bin.checkout_date.strftime('%m/%d/%y'), bin_path(bin) %></td>
            <td><%= link_to bin.agency.name, agency_path(bin.agency) %></td>
            <td><%= link_to bin.program.name, program_path(bin.program) %></td>
            <td><%= bin.num_of_bins %></td>
            <td><%= bin.get_total_value_of_bin %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>

  <!-- Add the pagination using Kaminari gem -->
  <span class="paginate">
    <%= paginate @bins %>
  </span>

</div>
