<%= form_for(@bin_item) do |f| %>
  <% if @bin_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bin_item.errors.count, "error") %> prohibited this bin_item from being saved:</h2>

      <ul>
      <% @bin_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :bin_id %><br>
    <%= f.number_field :bin_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
