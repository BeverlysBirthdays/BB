<div class='row'>

   <div class='col s12 m9 l10'>

    <!-- Nav buttons -->
    <div class = "row valign-wrapper">
      <h4 class="valign col s5">Items Listing</h4>
      <%= link_to "Add New Item", new_item_path, class: "waves-effect waves-light btn col s3 valign pink white-text" %>
      <%= link_to "Show Basket", show_cart_path, class: "waves-effect waves-light btn col s3 offset-s1 valign orange white-text" %>
    </div>

    <!-- Main Content -->
    <%= render( partial: 'list', locals: { items: @items} )%>
  </div> 

  <!-- Fixed filters on right side-->
  <div class='col hide-on-small-only m3 l2'>
    <br><p></p><br><br>
    <div class='toc-wrapper pinned card-panel'>
      <div class='buysellads hide-on-small-only'>
        <h5>Filters: </h5>
        <%= render( partial: 'filter_form' )%>
      </div>
    </div>
  </div>

</div>

<%# link_to 'Reset', reset_filterrific_url %>
<!-- 
<p id="notice"><%= notice %></p>
<div class="filter-box z-depth-2 green white-text">
  <%# render( partial: 'rough_filter_form' )%>
</div>
<%# link_to 'Reset', reset_filterrific_url %>
<%# render( partial: 'rough_index', locals: { items: @items} )%>  -->